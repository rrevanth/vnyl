/**
 * Spanish translations export
 * Combines all Spanish translation namespaces
 */

import common from './common.json'
import home from './home.json'
import search from './search.json'

export const es = {
  common,
  home,
  search,
  // Other namespaces can be added as they are translated
} as const

export default es