import React from 'react'
import { SettingsScreen } from '@/src/presentation/features/settings'

/**
 * Settings Tab Screen
 *
 * This is the tab navigation entry point for the Settings feature.
 * The actual implementation is in the Settings feature slice.
 */
export default function SettingsTabScreen() {
  return <SettingsScreen />
}